@model KurumAraDto
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    List<SelectListItem> iller = ViewData["iller"] as List<SelectListItem>;
    List<SelectListItem> okulTurleri = ViewData["okulTurleri"] as List<SelectListItem>;
    List<Blog> blogs = ViewData["Bloglar"] as List<Blog>;
    List<OzelDersOgretmenDto> ozelDersOgretmens = ViewData["OzelDersOgretmen"] as List<OzelDersOgretmenDto>;
    List<indirimDisplayDto> indirimler = ViewData["indirimler"] as List<indirimDisplayDto>;
    List<KurumDisplayDto> enCokZiyaretEdilenler = ViewData["EnCokTiklananlar"] as List<KurumDisplayDto>;
    List<BurslulukSinavDisplayDto> sinavlar = ViewData["Sinavlar"] as List<BurslulukSinavDisplayDto>;
}

<div class="row">
    <div class="card">
        <div class="card-body">
            <h3 class="header-title">ARAMA PANELİ</h3><br />
            <ul class="nav nav-tabs nav-bordered mb-3">
                <li class="nav-item">
                    <a href="#home-b1" data-bs-toggle="tab" aria-expanded="true" class="nav-link active">
                        @* <i class="mdi mdi-home-variant d-md-none d-block"></i>
                        <span class="d-none d-md-block">Okul Ara</span>*@
                        <span class="d-md-block">Okul Ara</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="#profile-b1" data-bs-toggle="tab" aria-expanded="true" class="nav-link">
                        @* <i class="mdi mdi-account-circle d-md-none d-block"></i>
                        <span class="d-none d-md-block">Kurs Ara</span>*@
                        <span class="d-md-block">Kurs Ara</span>
                    </a>
                </li>
            </ul>
            <div class="tab-content">
                <div class="tab-pane show active" id="home-b1">
                    @if (TempData["OkulAraUyari"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible bg-danger text-white border-0 fade show" role="alert">
                            <strong>Uyarı - </strong> @TempData["OkulAraUyari"]
                        </div>
                    }
                    @* <form class="form-vertical" data-ajax="true"
                    data-ajax-success="OnKurumAraSuccess"
                    data-ajax-begin="OnKurumAraBegin"
                    data-ajax-complete="OnKurumAraComplete" data-ajax-method="POST" asp-action="KurumAra" asp-controller="Home">*@
                    <form class="form-vertical" method="post" asp-action="KurumAra" asp-controller="Home">
                        <div class="row">
                            <input type="hidden" asp-for="OkulArama" value="true" />
                            <input type="hidden" asp-for="KursArama" value="false" />
                            <div class="col-lg-3">
                                <div class="mb-2">
                                    <select id="il1" asp-for="il1" class="form-control select2" data-toggle="select2">
                                        <option value="0">İl Seçiniz</option>
                                        @foreach (var item in iller)
                                        {
                                            <option value="@item.Value">@item.Text</option>
                                        }
                                    </select>
                                </div>
                            </div> <!-- end col -->
                            <div class="col-lg-3">
                                <div class="mb-2">
                                    <select id="ilce1" asp-for="ilce1" class="form-control select2" data-toggle="select2">
                                        <option value="0">İlçe Seçiniz</option>
                                    </select>
                                </div>

                            </div> <!-- end col -->
                            <div class="col-lg-3">
                                <div class="mb-2">
                                    <select id="okulTurId" asp-for="okulTurId" class="form-control select2" data-toggle="select2">
                                        <option value="0">Okul Türü Seçiniz</option>
                                        @foreach (var item in okulTurleri)
                                        {
                                            <option value="@item.Value">@item.Text</option>
                                        }
                                    </select>
                                </div>

                            </div> <!-- end col -->
                            @* <div class="col-lg-2">
                            <div class="mb-2">
                            <div class="form-check form-switch">
                            <input type="checkbox" asp-for="KurumYorumSecenekOkul" class="form-check-input" id="KurumYorumSecenekOkul">
                            <label class="form-check-label" for="KurumYorumSecenekOkul">En az 20 yorum olan kurumları listele</label>
                            </div>
                            </div>

                            </div> <!-- end col --> *@
                            <div class="col-lg-3 d-grid mb-2">
                                <button id="btnOkulAra" type="submit" class="btn btn-warning text-dark"><strong>Okul Ara</strong></button>

                            </div> <!-- end col -->
                        </div>
                    </form>
                </div>
                <div class="tab-pane show" id="profile-b1">
                    @if (TempData["KursAraUyari"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible bg-danger text-white border-0 fade show" role="alert">
                            <strong>Uyarı - </strong> @TempData["KursAraUyari"]
                        </div>
                    }
                    @*<form class="form-vertical" data-ajax="true"
                    data-ajax-success="OnKurumAraSuccess"
                    data-ajax-begin="OnKurumAraBegin"
                    data-ajax-complete="OnKurumAraComplete" data-ajax-method="POST" asp-action="KurumAra" asp-controller="Home">*@
                    <form class="form-vertical" method="post" asp-action="KurumAra" asp-controller="Home">
                        <div class="row">
                            <input type="hidden" asp-for="OkulArama" value="false" />
                            <input type="hidden" asp-for="KursArama" value="true" />
                            <div class="col-lg-4 mb-2">
                                <select id="il2" asp-for="il2" class="form-control select2" data-toggle="select2">
                                    <option value="0">İl Seçiniz</option>
                                    @foreach (var item in iller)
                                    {
                                        <option value="@item.Value">@item.Text</option>
                                    }
                                </select>
                            </div> <!-- end col -->
                            <div class="col-lg-4 mb-2">
                                <select id="ilce2" asp-for="ilce2" class="form-control select2" data-toggle="select2">
                                    <option>İlçe Seçiniz</option>
                                </select>
                            </div> <!-- end col -->
                            @* <div class="col-lg-3">
                            <div class="mb-2">
                            <div class="form-check form-switch">
                            <input type="checkbox" asp-for="KurumYorumSecenekKurs" class="form-check-input" id="KurumYorumSecenekKurs">
                            <label class="form-check-label" for="KurumYorumSecenekKurs">En az 20 yorum olan kurumları listele</label>
                            </div>
                            </div>

                            </div> <!-- end col --> *@
                            <div class="col-lg-4 d-grid mb-2">
                                <button id="btnKursAra" type="submit" class="btn btn-dark text-warning"><strong>Kurs Ara</strong></button>
                            </div> <!-- end col -->
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="card">
        <div class="card-body">
            @* @if (enCokZiyaretEdilenler.Count > 0)
            {
            <div class="float-end"><a href="@Url.Action("KurumListesi","Home",new {liste=enCokZiyaretEdilenler})" class="btn btn-warning btn-sm">Tümünü Gör</a></div>
            }*@
            <h3 class="header-title">EN ÇOK ZİYARET EDİLENLER</h3><br />
            <div class="row">
                @foreach (var item in enCokZiyaretEdilenler)
                {
                    <div class="col-6 col-sm-6 col-xxl-3">
                        <!-- project card -->
                        <div class="card">
                            <!-- project-thumbnail -->
                            <a href="@Url.Action("KurumDetay","Home",new {id=item.Id})">
                                <img class="card-img-top" style="height:200px;" src="@ByteToImage.DecodeImage(item.Resim)" alt="blog image cap">
                            </a>
                            <div class="card-body">
                                <div class="chart-widget-list mt-1 text-center">
                                    <p><strong>@item.Ad</strong></p>
                                    <p>@item.ilAdi.ToUpper()</p>
                                    <p>@item.ilceAdi.ToUpper()</p>
                                    <p><i class="mdi mdi-eye"></i> @item.TikSayisi Kişi İnceledi</p>
                                    <p><a href="@Url.Action("KurumDetay","Home",new {id=item.Id})" class="btn btn-sm btn-dark text-warning"><strong><i class="uil uil-corner-down-right"></i> İncele</strong></a></p>
                                </div>
                            </div>
                        </div> <!-- end card-->
                    </div>
                }
            </div>
            @if (enCokZiyaretEdilenler.Count > 0)
            {
                <div class="float-end"><a href="@Url.Action("KurumListesi","Home")" class="btn btn-warning btn-sm text-dark"><strong>Tümünü Gör</strong></a></div>
            }
        </div>

    </div>
</div>

<div class="row">
    <div class="card">
        <div class="card-body">

            <h3 class="header-title">ÖZEL İNDİRİMLER</h3><br />
            <div class="row">
                @foreach (var item in indirimler)
                {
                    <div class="col-6 col-md-3 col-xxl-3">
                        <!-- project card -->
                        <div class="card d-block">
                            <!-- project-thumbnail -->
                            <img class="card-img-top" style="height:200px;" src="@ByteToImage.DecodeImage(item.Resim)" alt="blog image cap">
                            <div class="card-body position-relative">
                                <div class="chart-widget-list mt-1 text-center">
                                    <p><strong>@item.KurumAdi</strong></p>
                                    <p>@item.ilAdi.ToUpper()</p>
                                    <p>@item.ilceAdi.ToUpper()</p>
                                    <p>Oran : <span class="badge badge-warning text-dark font-14">% @item.indirimOrani</span></p>
                                    <p>Son Başvuru : <span class="badge badge-warning text-dark font-14"> @Convert.ToDateTime(item.SonTarih).ToShortDateString()</span></p>
                                </div>
                            </div>
                        </div> <!-- end card-->
                    </div>
                }
            </div>
            @if (indirimler.Count > 0)
            {
                <div class="float-end"><a href="@Url.Action("indirimListesi","Home")" class="btn btn-warning btn-sm text-dark"><strong>Tümünü Gör</strong></a></div>
            }
        </div>

    </div>
</div>

<div class="row">
    <div class="card">
        <div class="card-body">
            <h3 class="header-title">BURSLULUK SINAVLARI</h3><br />
            <div class="row">
                @foreach (var item in sinavlar)
                {
                    <div class="col-6 col-md-3 col-xxl-3">
                        <!-- project card -->
                        <div class="card d-block">
                            <!-- project-thumbnail -->
                            <a href="@Url.Action("SinavDetay","Home",new {id=item.Id})">
                                <img class="card-img-top" style="height:200px;" src="@ByteToImage.DecodeImage(item.Resim)" alt="blog image cap">
                            </a>
                            <div class="card-body position-relative text-center">
                                <div class="chart-widget-list mt-1 text-center">
                                    <p><strong>@item.KurumAdi</strong></p>
                                    <p>@item.ilAdi.ToUpper()</p>
                                    <p>@item.ilceAdi.ToUpper()</p>
                                    <p>@item.SinavAdi</p>
                                    <p>Son Başvuru : <span class="badge badge-warning text-dark font-14">@Convert.ToDateTime(item.SonBasvuruTarih).ToShortDateString()</span></p>
                                    <p>Sınav Tarihi : <span class="badge badge-warning text-dark font-14">@Convert.ToDateTime(item.SinavTarih).ToShortDateString()</span></p>
                                    <p>Sınav Saati : <span class="badge badge-warning text-dark font-14">@item.SinavSaat</span></p>
                                    <p><a href="@Url.Action("SinavDetay","Home",new {id=item.Id})" class="btn btn-sm btn-dark text-warning"><strong><i class="uil uil-corner-down-right"></i> İncele</strong></a></p>
                                </div>
                            </div>
                        </div> <!-- end card-->
                    </div>
                }
            </div>
            @if (sinavlar.Count > 0)
            {
                <div class="float-end"><a href="@Url.Action("SinavListesi","Home")" class="btn btn-warning btn-sm text-dark"><strong>Tümünü Gör</strong></a></div>
            }
            else
            {
                <div class="alert alert-danger" role="alert">
                    <strong>Uyarı - </strong> Aktif Bursluluk Sınavı duyurusu yok !
                </div>
            }
        </div>
    </div>
</div>

<div class="row">
    <div class="card">
        <div class="card-body">
            <h3 class="header-title">EĞİTİM İÇERİKLERİ / BLOG</h3><br />
            <div class="row">
                @foreach (var item in blogs)
                {
                    <div class="col-6 col-md-3 col-xxl-3">
                        <!-- project card -->
                        <div class="card d-block" style="max-width:250px;">
                            <!-- project-thumbnail -->
                            <a href="@Url.Action("BlogDetail","Home",new {id=item.Id})">
                                <img class="card-img-top" style="height:200px;" src="@ByteToImage.DecodeImage(item.Resim)" alt="blog image cap">
                            </a>
                            <div class="card-body position-relative">
                                <div class="card-body position-relative text-center">
                                    <!-- project title-->
                                    <p>
                                        <a href="@Url.Action("BlogDetail","Home",new {id=item.Id})" class="text-title text-dark"><strong>@item.Baslik</strong></a>
                                    </p>
                                    <p>
                                        <span class="pe-2 text-muted">
                                            <span class="badge badge-warning-lighten text-dark font-12">
                                                <strong>
                                                    @item.AdSoyad
                                                </strong>
                                            </span>
                                        </span>
                                    </p>
                                    <p class="text-muted font-13">
                                        @item.Aciklama
                                    </p>
                                    <!-- project detail-->
                                    <p>
                                        <span class="badge badge-warning-lighten text-dark font-12">
                                            <i class="mdi mdi-eye"></i><strong>
                                                @item.Hit kez görüntülendi.
                                            </strong>
                                        </span>
                                    </p>
                                    <p><a href="@Url.Action("BlogDetail","Home",new {id=item.Id})" class="btn btn-sm btn-dark text-warning"><strong><i class="uil uil-corner-down-right"></i> İncele</strong></a></p>
                                </div>
                            </div> <!-- end card-body-->
                        </div> <!-- end card-->
                    </div>
                }
            </div>
            @if (blogs.Count > 0)
            {
                <div class="float-end"><a href="@Url.Action("BlogListesi","Home")" class="btn btn-warning btn-sm text-dark"><strong>Tümünü Gör</strong></a></div>
            }
        </div>
    </div>
</div>

<div class="row">
    <div class="card">
        <div class="card-body">

            <h3 class="header-title">ÖZEL DERS VERENLER</h3><br />
            <div class="row">
                @foreach (var item in ozelDersOgretmens)
                {
                    <div class="col-6 col-md-4 col-xxl-3">
                        <div class="card text-center">
                            <div class="card-body">
                                <a href="@Url.Action("ProfileDetail","Home",new {id=item.Id})">
                                    <img src="@ByteToImage.DecodeImage(item.Resim)" class="rounded-circle avatar-lg img-thumbnail" alt="profile-image">
                                </a>
                                <h4 class="mb-0 mt-2" style="height:50px;"><a href="@Url.Action("ProfileDetail","Home",new {id=item.Id})">@item.AdSoyad</a></h4>
                                <p class="text-muted font-14">@item.BransAdi</p>
                                <p class="text-muted font-14">@item.ilAdi</p>

                                @if (item.Online)
                                {
                                    <button type="button" class="btn btn-success btn-sm mb-2" disabled>Online</button>
                                }
                                else
                                {
                                    <button type="button" class="btn btn-danger btn-sm mb-2" disabled>Online</button>
                                }
                                @if (item.Yuzyuze)
                                {
                                    <button type="button" class="btn btn-success btn-sm mb-2" disabled>Yüzyüze</button>
                                }
                                else
                                {
                                    <button type="button" class="btn btn-danger btn-sm mb-2" disabled>Yüzyüze</button>
                                }
                            </div> <!-- end card-body -->
                        </div> <!-- end card -->
                        <!-- Messages-->
                    </div>
                }
            </div>
            @if (ozelDersOgretmens.Count > 0)
            {
                <div class="float-end"><a href="@Url.Action("OzelDersOgretmenListesi","Home")" class="btn btn-warning btn-sm text-dark"><strong>Tümünü Gör</strong></a></div>
            }
        </div>
    </div>
</div>

@section Scripts{

    <script>

        $(window).on('load',
            function () {
                $('#loading').hide();

            });
        //var OnKurumAraBegin = function () {
        //    $("#btnOkulAra").prop("disabled", true);
        //    $("#btnOkulAra").html('Lütfen Bekleyin...');
        //    $("#btnKursAra").prop("disabled", true);
        //};
        //var OnKurumAraComplete = function () {
        //    $("#btnOkulAra").prop("disabled", false);
        //    $("#btnOkulAra").html('Okul Ara');
        //    $("#btnKursAra").prop("disabled", false);
        //}
        //var OnKurumAraSuccess = function (data) {
        //    if (data.Success) {
        //        toastr.success(data.Data.length + " adet kayıt bulundu.");
        //        console.log(data.Data);
        //        //window.location.href = "/Home/KurumListesi/" + JSON.stringify(data.Data);

        //        var link = '@Url.Action("KurumAraListe", "Home", new { liste = "-1" })';
        //        link = link.replace("-1", JSON.stringify(data.Data));

        //        window.location.href = link;
        //    }
        //    else {
        //        swal({
        //            title: 'UYARI',
        //            text: data.Message,
        //            type: 'error',
        //            confirmButtonClass: 'btn-danger',
        //            confirmButtonText: 'Tamam',
        //        });
        //    }
        //};

        //var OnKursAraBegin = function () {
        //    $("#btnKursAra").prop("disabled", true);
        //    $("#btnKursAra").html('Lütfen Bekleyin...');
        //    $("#btnOkulAra").prop("disabled", true);
        //};
        //var OnKursAraComplete = function () {
        //    $("#btnKursAra").prop("disabled", false);
        //    $("#btnKursAra").html('Kurs Ara');
        //    $("#btnOkulAra").prop("disabled", false);
        //}
        //var OnKursAraSuccess = function (data) {
        //    if (data.Success) {
        //        //swal({
        //        //    title: 'BAŞARILI',
        //        //    text: "Tebrikler. Okul bilgileriniz eklendi. Hesabınız onaylandıktan sonra sistemi kullanmaya başlayabilirsiniz. Onaylama işlemi sonrasında belirtmiş olduğunuz e-posta adresine bilgilendirme postası otomatik olarak gönderilecektir.",
        //        //    type: 'success',
        //        //    confirmButtonClass: 'btn-info',
        //        //    confirmButtonText: 'Tamam',
        //        //}, function (isConfirm) {
        //        //    if (isConfirm) {
        //        //        window.location.href = '@Html.Raw(Url.Action("Index","Home"))';
        //        //    }
        //        //});
        //    }
        //    else {
        //        swal({
        //            title: 'UYARI',
        //            text: data.Message,
        //            type: 'error',
        //            confirmButtonClass: 'btn-danger',
        //            confirmButtonText: 'Tamam',
        //        });
        //    }
        //};

        $(document).ready(function () {
            $('#il1').on('change', function () {
                var id = $(this).val();
                var ilceListe = $("#ilce1");
                ilceListe.empty();
                $.ajax({
                    url: '../Home/GetSelectListilce/',
                    type: 'POST',
                    dataType: 'json',
                    data: { 'ilId': id },
                    success: function (data) {
                        $.each(data, function (index, option) {
                            ilceListe.append('<option value=' + option.Value + '>' + option.Text + '</option>');
                        });
                    }
                });
            });
        });


        $(document).ready(function () {
            $('#il2').on('change', function () {
                var id = $(this).val();
                var ilceListe = $("#ilce2");
                ilceListe.empty();
                $.ajax({
                    url: '../Home/GetSelectListilce/',
                    type: 'POST',
                    dataType: 'json',
                    data: { 'ilId': id },
                    success: function (data) {
                        $.each(data, function (index, option) {
                            ilceListe.append('<option value=' + option.Value + '>' + option.Text + '</option>');
                        });
                    }
                });
            });
        });
    </script>
}