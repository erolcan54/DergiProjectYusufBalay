@model List<Slider>

@if (Model.Count > 0)
{
    <div class="row mb-3">
        <div class="col-12">
            <div class="row">
            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                            <img src="@ByteToImage.DecodeImage(Model[0].Resim)" style="min-height:100px;max-height:200px; max-width:100%;overflow:auto; " />
                    </div>
                    @if (Model.Count > 1)
                    {
                        for (int i = 1; i < Model.Count; i++)
                        {
                            <div class="carousel-item">
                                <img src="@ByteToImage.DecodeImage(Model[i].Resim)" style="min-height:100px; max-height:200px;max-width:100%;overflow:auto;" />
                            </div>
                        }
                    }
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
    </div>
    </div>
}